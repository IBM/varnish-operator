bases:
  - ../rbac
  - ../crds

patchesStrategicMerge:
- crd_patch.yaml

patchesJson6902:
- target:
    group: rbac.authorization.k8s.io
    version: v1
    kind: ClusterRole
    name: manager-role
  path: role_patch.yaml
